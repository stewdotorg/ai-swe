# Agent Architectures Explained: ReAct, Planning, Memory, Workflows, and Multi-Agent Systems

| | |
|---|---
| Creator | Mohamed Achaq
| Published | 2026-05-04
| Kind | captions
| Language | en
| Source | [https://www.youtube.com/watch?v=D7Wbwb7jJKs](https://www.youtube.com/watch?v=D7Wbwb7jJKs)
| Generated | 2026-05-04

## Description

Agent architectures make more sense when you stop looking at the names and start looking at the loop.
In this video, I walk through the main patterns behind modern AI agents: ReAct, reactive tool use, plan-and-execute, reflection, memory-augmented agents, workflow/graph agents, router agents, supervisor-worker systems, multi-agent collaboration, human-in-the-loop, and hybrid systems.
The focus is practical: what the agent sees, what it can do, what happens after it acts, who checks the result, and when the loop stops.
Useful if you’re building agents, evaluating frameworks, or trying to understand where these architectures actually differ.
Chapters:
00:00 Introduction
00:40 Basic agentic loop
01:40 ReAct agent
02:27 Reactive tool-using agent
03:06 Plan-and-execute agent
03:39 Reflection agent
04:08 Memory-augmented agent
04:49 Graph / workflow agent
05:51 Router / specialist agent
06:31 Hierarchical supervisor-worker agent
07:27 Multi-agent collaboration
08:01 Human-in-the-loop agent
09:13 Hybrid agentic systems
10:15 Architecture decision tree
11:31 Closing
Follow / Connect:
LinkedIn: https://www.linkedin.com/in/chaqchase
X: https://x.com/chaqchase
GitHub: https://github.com/chaqchase
#AI #Agents #LLM #SoftwareEngineering #AgenticAI

> Transcript extracted from YouTube auto-generated captions (`D7Wbwb7jJKs.en.vtt`) and cleaned with `vtclean.py`. Timestamps have been removed; paragraph breaks follow sentence boundaries (or pauses > 1.5 s). Minor transcription artifacts from the automatic speech recognition may remain.

## Transcript

Hi everyone and welcome to a new video.

The topic of today's video is agent architectures and how the agentic loop works. Uh and in this video we will learn about 12 agent architectures and uh like the guidelines to choose which one is best for your uh use case. The first thing we need to establish is the model model matters and the agency group matters even more. That's why even if you have different uh harnesses like let's say for example cursor and the cloud code and oper code the same model behaves different in a different way in each of of them uh even though they are using the same model that's why the agentic loop uh can make or break your uh entire agentic system before we dive deeper into this topic I want to establish a little bit of vocabulary for for this video the first thing is the agent is the thing that try to complete a task you give it a task you wait for it you get a results back the harness is the runtime that lets your agents actually operate and do the actual work to complete the task and the agent architecture is the design of the whole system around the agent. The first thing let's check how uh like a basic agentic loop works. The agentic loop uh consists uh in most cases uh of at least five uh steps. The first one is uh loading states like uh this includes uh gathering the context defining the goal uh loading history of the conversation.

Then the second step is deciding what should happen next uh and that's can be either uh taking an action or returning an answer back to the user. The third step is uh taking an action usually and that consists of calling tools, calling external APIs u and doing the actual work to accomplish the task. Then the agent observes and derived the deres the final uh state if it's a result or an error. Then based on that we uh continue or we deliver the final answer to the user.

Now I prepared like 12 12 ways you can architect your your agents. Let's talk about the first type of agent architectures which hold the react agent. And this kind of agent makes a reasoning and action in the same loop.

Hence the name react agent. And it starts by defining the user goal. Then the agent or the model thinks uh about what are the next steps for this uh agent. Then takes actions like calling tools, calling external APIs etc. Then observes the results or uh like the error based on this final state of the agent. Then uh it generates the final answer once it gets the to the final uh once it accomplishes the goal uh or it completes uh the like uh subsequent uh uh loops to accomplish the task. And this is the simplest uh and most effective uh way to uh architect your agents for 99% of of use cases. Then we have a reactive tool using uh agents.

This is like um also a simple uh kind of agent and uh it does not build uh a full plan before it starts working. Uh it's it just uh takes the current state and uh like runs tools based on that and once it achieves the desired state of the that system uh it it returns a final answer to the user. You can find this like in the majority of coding agents because like when you ask an agent to create a new feature uh it takes a look at the code and loads like snippets of of of the files or inside files to understand like what is the current state of that uh system or that code base. Then it calls tools and makes edits to the files so it can accomplish the final uh user goal. Then we have uh the plan and execute agent architecture.

This is like a plan based uh architecture which consists of uh the first step being uh generating a higher level plan and that plan includes uh steps. Then you have an executor uh or some kind of uh exe execution logic that's uh calls tools executes code etc and uh it achieves uh like a result. The agent observes the outcome then uh it makes it either makes a new plan or uh just uh answers directly to the user if the find if the uh task uh is accomplished.

Then we have a reflection uh agent. The reflection agent uh improves its own output before uh returning. So it's it drafts uh an output attempt. Then you have uh it can this can be a second agent that's a critique for for that uh uh output or that draft. Then it gives feedback to the like the agent that created the draft. Uh it improves the quality and so on and so forth. Uh until it's a good enough output to deliver to the final user. Then we uh just answer to the fin to the user with the finalized answer. that's polished. Then we have uh a memory augmented agents uh which uh uses uh earlier interactions and learnings from uh like uh the conversation history to uh improve its own uh execution loop. And like it starts as always it starts with a user goal. Then it loads the necessary context like uh session states uh recent messages etc. Then retrieves and finds any uh relevant memories from the system. then reasons and acts uh and after this step like it becomes like a react agent until the final step instead of just returning the answer directly to the user it runs check on the the entire conversation history and finds any relevant uh memories uh so it can be stored so it can be used later for for subsequent uh runs after that we have a graph a graph agents or a graph agentic system this kind of uh agents like uh puts control flow into uh the graph instead of letting like the the model decide uh every next step freely the system moves uh through defined nodes like classify uh a classifier agents like a search agents a summarization agents etc. So as always like it starts with uh initializing the data workflow then it classifies the request to and chooses a path uh then tries to search and retrieve the necessary information summarize those uh informations and send synthetize the the find findings uh then verifies the quality and uh does a check on the policy if it passes or fails and if it passes it outputs the results if it fails it tries and repairs the the entire workflow so it can fix the issues. uh this is permanent for for those uh search uh deep uh research agents uh like the one uh that exists in pretty much uh any uh big lab uh application like uh uh cloud etc. They all have uh deep resource agents that don't involve like the uh the same uh agentic workflow like you see the agents walking. It does all that work in the background in uh subsequent steps that are predefined uh ahead of time.

Then we have a router/ specialist agent architecture. Uh this like uh is prominent for the agent systems that involve uh different uh agents working at the same time uh to accomplish uh the same goal. It starts as always with a user request. Then the router like classifies the request and selects a specialized agent. Uh for instance like it can select a research agent uh to find a sitize information. Uh a coding agent to write and debug code. Uh writing agents to draft edits and define the the final uh the content or the final answer to the user. Uh and a data agent to analyze visualize and the model data. Then after like all those agents completes their work the then it comes like the role of the aggregator which combines and vehicles all the results to draft a final answer in the user.

Then we have hierarchical uh supervisor worker agents. Uh this like uh is similar to how we as humans work. You have uh devs, you have products manager and you have like uh like uh shareholders etc. And uh all the the requests that come from the top are uh divided and uh routed to uh like specialized uh like workers that do the actual work and that work is validated by uh products manager. Then it deliver it's delivered at the end to the uh stakeholders. So as always it starts with a user go or a user request. Then you have a super supervisor agents that plans and assigns and monitors the worker agents. And the worker agents can be any type of worker agents like based on your uh use case. Uh it can be uh like a research agents uh coding agents and design agents etc. And the supervisor like assigns uh tasks to each of each one of them. Uh and uh once that's done the supervisor uh reviews everything and synthesizes and combines and checks and decides if that's good enough to fulfill the user goal or not.

Uh and so on and so forth. Uh then we have a multi- aent uh collaboration uh system. Uh this like uses uh beer uh agents instead of having a hierarchical approach to the the system. So you have uh like the same all those agents have a shared workspace uh where they store uh notes, facts, memories, opening questions, tasks etc. And each one of them like uh takes uh tasks from that shared workspace and tries to uh accomplish them based on their qualifications, based on their uh skills. uh and after like uh all of them have done the work we have uh all of that work uh is like passed to a synthesizer agent which resolves conflicts and combines work to deliver a cohesive final answer to the user. Then we have uh one of the most requested agents uh basically in any uh app currently uh that try to uh integrate AI into their uh products uh for example like Salesforce like uh uh SAP etc. uh they they had like users had uh like predefined workflows before AI was a thing and all of those workflows require approval and like deal with sometimes uh sensitive uh data and like there is a a little bit of room for for risk or like uh taking the the wrong path in resolving a ticket. That's why we need a human in the loop agent. So as always it starts with a user goal then uh the agent plans and drafts uh proposal uh for for the the actions that need to be taken. Then we have like before each to call or each like u step that requires like uh access to data uh there is uh an approval approach that the user sees uh then he can approve uh or disapprove the like the action from being taken. Uh if the user disapproves like the the entire plan is revised and the agent tries to uh modify it so it can pass the human approval. Uh if it's approved it will be executed and the loop continues till we finally uh achieve the user goal and we deliver the final answer to the user.

Then we have a hybrid uh aentic system like most real uh agentic systems like combine separate patterns uh like it can include like different kind of of architectures in the same system. uh it can for example have uh planner based agents uh like that's uh working with a react agent or a react agent is included into the entire workflow of the planner agents uh and that plan agents can be part of of a hierarchical uh system or appear based uh agentic system and that's how like uh most complex agentic system systems actually work as always the enticing loop starts with a user request that's given to the router then the router either plans and execute uh for for long running tasks uh if task is complex enough or reactively uh does works on the on the task until uh it's uh into a react to execution loop. Then uh like the entire uh system has a reflection step. So it can check the the quality of of the work. Uh if uh risky action needs to be taken uh it's been like routed to a human for approval. Uh and once it's approved it's executed. Uh if none of the tasks like achieve a risk uh threshold like the agent look continues until we finally uh achieve the final uh result. Now the question becomes how how I can decide what is the best agentic architecture for my uh use case and for that I prepared a decision tree that can help you just with that.

So you start with the task. If the task is simple and local then use a reactive to using agent uh similar to how clowork uh works like clo is essentially a reactive uh tool using agent that drives uh locally. If the task needs tools and feedback you use our ex agent which combines reasoning and action at the same time in the same agent loop. If the task is long running or multi-step, you use a plan based uh agentic system uh which blends the like the subtasks then executes them until the goal is achieved. If it needs uh quality improvements uh at runtime, you use a reflection agent which does the entire work and reflects on the quality of the output. If it's good enough, it delivers the final uh synthesis synthesize the answer to the user. If not, it continues the agent tech loop. If the task needs rare context then you use a memory augmented agent which retrieves uh memories that are stored in previous runs uh for like enhancing or uh not redoing the entire work again in the current. If the task like can include risky actions then uh you use a human in the loop agents. If not you can just uh use a simple workflow uh with a direct answer once all those actions have run.

And this concludes our video for agentic architectures and how to decide which is the best uh agentic architecture you can use for your for your given use case.

And uh thank you for watching and stay tuned for uh future videos uh where I will be explaining uh more concepts and uh like how you can optimize your decisions working uh and architecting uh agency systems in the future. Thank you
