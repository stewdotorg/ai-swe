# Pi Agent (Full Course)

| | |
|---|---
| Creator | Vilson Vieira
| Published | 2026-04-29
| Kind | captions
| Language | en
| Source | [https://www.youtube.com/watch?v=yAPKzHrx3eo](https://www.youtube.com/watch?v=yAPKzHrx3eo)
| Generated | 2026-05-04

## Description

This course gives you everything you need to use and extend Pi as your main AI coding agent. No fluff, hands-on, we'll go deep into Pi, how to create extensions, install and use packages, and how to integrate Pi into your own apps.
Chapters:
0:00 - 0:31: Intro
0:31 - 6:15: What Pi is (and what it isn't)
6:16 - 6:49: Installation
6:50 - 10:47: Providers, Authentication and models
10:48 - 11:56: Useful keystrokes and commands
11:57 - 12:44: Directories
12:45 - 17:58: Sessions
17:59 - 23:42: Context Engineering
23:43 - 27:45: Extensions
27:46 - 28:50: Installing Pi packages
28:51 - 29:51: Themes
29:52 - 32:34: Integrating Pi in your app
32:35 - 32:52: Documentation
32:53 - 34:06: Setup script, forks and other flavors
34:07 - 34:47: Outro
Pi official website:
https://pi.dev
About how to install Pi and use its SDK:
https://www.youtube.com/watch?v=hV_hnPJ2r6k
Pi Docs:
https://pi.dev/docs
Pi Extensions and SDK examples:
https://github.com/badlogic/pi-mono/tree/main/packages/coding-agent/examples
LazyPi: https://lazypi.org/
Oh My Pi: https://github.com/can1357/oh-my-pi
Pi Rust: https://github.com/Dicklesworthstone/pi_agent_rust
Modex: https://github.com/automata/modex
My startup: https://teragon.cc
#piagent #ai #aicoding #vibecoding #artificialintelligence #opensource #typescript #python #programming #softwareengineer #software #softwaredevelopment #pi-coding-agent

> Transcript extracted from YouTube auto-generated captions (`yAPKzHrx3eo.en.vtt`) and cleaned with `vtclean.py`. Timestamps have been removed; paragraph breaks follow sentence boundaries (or pauses > 1.5 s). Minor transcription artifacts from the automatic speech recognition may remain.

## Transcript

Today is a impressive sunny day in London. So I think it's a really great day to talk about Pi. We have like lots to cover. I I prepared like a kind of like this full course, full guide through PI agent. If we finish this together, you are going to be in a really great state to use PI and adapt PI for whatever need that you have because that's one of the best qualities that Pi has. It is really hackable and you can tweak it for your own needs. So let's jump straight to it first like uh what pi is and what is not. So pi is pretty minimal. It's like uh when you take like cloud code or other agent and you strip out everything else and you just skip the core. Even the system prompt of pi is pretty minimal. And I create a little extension here uh that I call system prompt. I created this comment here that just like shows the the current system prompt that we have loaded and you can see that it's really really minimal like you are an expert coding assistant. That's it.

That's just like one sentence to describe what Pi is the available tools that Pi has and it's basically four tools. It's really minimalist. So you can read, edit and write files and execute batch comments.

Uh why is that? Well, those code agents, they are already like trained uh during especially during post training when you have this reinforcement learning feedback when users go through the whole process of editing, fixing bugs, editing search files. So all that is already inside of the learning of the model. So it doesn't make sense to increase our context window with tons of uh tokens.

If we compare the the size of the system prompts with other tools like cloud code, it's like orders of magnitude less than um than what we have at PI. There's no MCP support uh by default. MCPS are really interesting when you have a specific needs. For instance, when you want to communicate your agent with a browser, for instance, like that's really interesting to use MCP, but MCP burns a lot of context window. um you can go up to 10k tokens, 10,000 tokens per server that you add as an MCP server. And so imagine that you have like uh five to 10 MC MCP servers are configured. You're going to use half of your context window only for MCP definitions. So I think it's not interesting to use that much only when you have a specific need. And if you really want to use that then you can use something like PI MCP adapter or MCP portter. And those tools will allow you to connect through um PI to MCP or rapid MCPS CLI 2 like the MCP portter. And another option instead of using MCP you can use that as a skill which is not more than a CLI 2 in some instructions how to use that CLI too. there is no subie agents in in pi. So if you want to run pi and spawn many subie agent just tell pi to start other pi instances um if you want to and one interesting aspect of sub agents is that when you use a sub agent at least on the context that have been using in cloud codes for instance is generally because you didn't plan ahead. So for instance you you're going to spawn an subie agent to check some documentation for the for the development that your agent are doing.

But you don't need to do that because if you plan ahead and you know where documentations is you just point there before and then the the agent that you already have will just like go through that and read the documentation. You don't need the sub agent or you can just build your own support for sub agents.

You can ask pi to do that or install package. There are lots of packages too.

I'm going to cover that later as well.

There's no permission blocks neither.

Um, so you don't need to use all the time cloud escape dangerously permissions anymore. So pi is yolo by default. Why? Because if you need security restrictions, it's not that you know like um flow of uh hey I need to run some comments please allow it or not. and that running back and forth that's not the best way to design uh security around your PI agent. There's many other ways to do that like using sandboxes using some other guards that you can use. So if you want security you can do that uh through some extensions as well. There's no plan mode or to-do mode. You can do that by actually creating your plan file or whatever name you want to use for your plan or a to-do MD file markdown file.

And I do that for instance all the time.

I don't feel the need to have a plan or to do as an extension or if you want to use it you can create your own as well and there is no background bash uh pi has like this main thread it runs everything as in that thread and um if you want to spawn other comments inside bash you can use timax for instance because inside pi you can say hey you are allowed to use timax uh to spawn other sessions use it while doing this task while running the comment and Basically we create a tim session add the bash there and use timax that's the beauty of using bash in unix systems because everything is a file everything is a common that runs on and do some task really really well so it's just a matter of u depending on that power using that power in your your favor and it's really great also for observability because if you run something in the background bash and you want to monitor what that specific comment is doing you're going have had to develop lots of monitoring around that. When you use TMX, it's just a matter of connecting attaching to that specific TMX session and you can have like the full terminal and explore what's happening there. And of course, if you're not happy with that, you can also like uh extend PI again and uh and have something on your own face. Okay, so let's start and go for the installation. Um so to install, it's really simple. You just have to copy this com here. You can go, by the way, in uh pi.dev and copy the command here too. There you go. It install pi. To run it, you just type pi. So when you're present to pi the first time, you're going to see the version. Um you can type ctr o for to see more comments that pi has more comments here. And there's lots of comments that we're going to cover. Once you have pi installed, the first thing that I generally do is to set up my providers. So how I'm going to access the large language models that I have access to. First difference between pi and cloud code and codex is that you are free to choose you. You are not entangled to a specific provider uh like openai or entropic. To log to your provider, you just have to go um execute pi and type login. I generally use my CHP plus and pro account. Select here.

It will open the browser. When you got back to that flow, you're going to see here that your subscription is configured and um from now on you'll be able to use um the JDP module using your codec subscription. So the other way is to instead of using your subscription, you can use an API key of any provider that you want. One really great way to do that is using open router. And to do that, you have to create an account in open router. So just open router.ai.

And if you go for uh credits, you can create a add a bit of credits here. Um you know like 10 bucks to start for instance. Then you go to API keys and then create a new API key for you. When you create an API key, it's going to generate it. You can copy the key and then export it as open router API key.

And here you type your token.

Once you do that, it's going to register this environment variable in your environment and um you'll be able to access open router. Another option is to edit this file and we are going to cover more of uh all the directories that we have in PI installations for configuring it.

But if you go to authentication, you see here the the providers that we already configured. So we see the open ic. I already have my open router key as well.

And once you have open router configure now you can go back to pi use the model common command that I use all the time and type open router. You see they have like more than 200 models available for you to use and let's say that I want to use GLM 5.1 and I'm going with that. You see on the status here that now we have open router as a provider with the jo 5.1 model and we can use it uh for whatever you want.

If you want to go back to GPT, you just type GPT um 5.5 for instance. Select the openi codeex one. That's the your subscription and uh you're now in codex land. In this way, you can combine different models in any way you want. You can start with one model, switch for the other, have like sessions starting with one model, then you can fork the session, branch out and then continue with another model. It's really powerful and really you know you can feel the freedom of using any model that you want. There's another important point here. You can also use local models and it's going to start happening more and more especially now that we have open source models that can run locally and for AI coding. To use a local model you just go to pi agent models.json JSON and you can use um Olma for instance to run your model in some specific point like the default one in LMA it specify the model that you want the context window that and you're going to have that model also supported in pi so it is kind of like this Swiss army knife of models you can use whatever model you want uh there's a lots of uh key strokes and comments that exist in in pi and um those are kind of the ones that use most important one is how to exit pi. So if you are running pi you type ctr ctrl c and just exit it or if there is anything running in uh for instance like some model running and you want to cancel you can just like type ctrl c and that will interrupt the model. Other stroke that is handy is shift enter. So if you are typing for instance like a large typing a large plan here and I want more lines I can just type shift enter and it will not send the prompt but will allow you to add more lines to your prompt. And also if you type it something you want to clean it up you just like type Ctrl C that will clean that uh the whole prompt for you.

double ask is it will bring you to tree mode which we're going to discuss next.

So it's the same thing as typing tree and you're going to see that there is like lots of other keystrokes. Um I cover like a bit how to access the documentation of pi. So you can check all the other keystrokes that we have there. So everything that you configure in pi or install is going to be inside of this home.py/ aent folder. All the extensions, skills, prompts, teams, sessions, models or JSON or settings JSON that you have for PI are going to be there. If you want to specify or overwrite the system prompt globally in your computer, you can also use system there and that will be overwriting the uh the full system prompt that comes with PI. But if you want a specific agents for instance for your project, you can add that in the root folder of your project or inside root.py agents.mme for instance. Okay, let's cover now a pretty important topic which is sessions. In PI, every message that you exchange between you and your agent are going to be stored in this pi agent sessions. So if we go inside upi agent sessions, we're going to have one folder per each project or the folder that you just started a pi session. And if we enter

folders, we're going to see a bunch of JSON L files. And if we check the content of one of those files, you're going to see just basic JSON L file and every single operation that we did during the session even like the model changes are going to be available in this session file. So it's really convenient because if you want to inspect the sessions later through some other tool that you have, you can do that just by accessing those files. is pretty transparent. So just like in chatpt when you have this like main thread of chat and you can branch out and you can keep like the conversation on the original branch and start a new adding new messages on the new branch. You can do exactly the same thing in pi. So for instance, if I start a new PI session here and uh I type tree and tree is the main uh navigation common that you're going to have. If we go back and add some messages like u list the files in this folder. So it listed the files and if I type tree now I'm going to see the messages that uh I entered before. So let's say that I want to branch out from this message here. So I basically go there, type enter and it will ask me to summarize or not that branch. So I don't want summary. Uh so let's see list only the folders. And now if we type tree, we actually have a tree. So that's a really powerful feature because you can create like this complex trees or exploring some specific details of your codebase or some tests that you want some experiments that you want to have and select what kind of context that you want to go with. Another important comment is for and with that we are going to create a completely new session from a previous user message. We only had those two messages. So I can start with this one for instance. It's going to be a completely new session. That's why we don't have like any comments from the previous session that we had before. And you might be asking, okay, how I go back to that? How can I see all the sessions? There's two ways to do that. One is I minus C will basically continue our last session. And uh you see here that's exactly the session that we started before. You can use pi minus r that will restore a specific session that you can select. So here you have like all the sessions that we have so far in this folder. And this is really really cool because we can just like keeping adding sessions, keeping starting pi but we can go back to that. I have like sessions here from six days ago and it's okay. I can just like go back to that and I have exactly the session that I kept when I left and I'm just restoring for it. Another cool comment is share and if you have the GitHub CLI configure that will create a secret guest that you can share with your peers that your colleagues or whoever are like working in your project.

And another one is export. Uh so if you type export the session will be exported to this file here. So just like a HTML file. So if you go ahead and open this file you're going to see the tree like the message that we had before pointed and also how all the other messages or everything that is inside your context.

If you are in the middle of a session and you want to start a new one, you just type new. that will start a completely fresh session. And if you are in a session and you want to know how much that costs and how much tokens were consuming, you can type session and that will give you all the stats that you want to know about your um session like a number of tokens use. In this case, I consume like more than 700,000 tokens and costes 48 cents for me. So that's it. Sessions are pretty important. Uh easy to navigate with those comments like new, three, fork, share, export, and session to know the stats and pi minus c, pi minus r if you want to continue restore the session.

Cortex engineering is one of the main topics not only in PI but uh in any agent that you use because that's how you can select what you are going to add to your context window uh provide tools for your agents to do a great job. Pi has some ways to do context engineering and the first one is through agents.mmeg and that's not only for PI but for other agents as well. It's basically where you put everything related to tech guidelines, important source files or documentations that you want to bring to the agent like architecture or design decisions that you have. It's also the place where you can instruct the agent on which tools he can use to run tasks or to run the app uh by itself. You can place agents.mme in the root of your project folder or inside this subfolder.py.

It's equivalent the same thing.

And if you want that agent MED to be applied for all the sessions that you have, you can also put that in your home PI agent folder. Yes, the same is valid for the system and the pend system and other files that you can have that applied globally in your system. And just to give an example of how an agent file can look like uh let's open the one that is used in pi itself. So here you can see the guidelines for the agent. So covering uh code quality, the comments that you can run to test uh pi, how to do PRs and how to test in interactive mods using t-max and other comments. So it's really like a way to guide your agent for the best practices on how to develop your project. Another important file is system.md.

And if you use that inside of your folder or inside of PI agent systemd, you're going to completely replace the original system prompt of PI. That's really great because it's really flexible. You can also call that through the system prompt argument in PI. And here I'm just like saying pay you are a code reviewer. And I'm providing tools that are only read to there's no write permission here. So let's see uh how it goes. So if I ask it okay create a Python script with hello world we check the output of this agent we'll see that uh it's saying that the user asked it for creating script but it can do it.

It's only a reviewer of code and doesn't has like any tools to to write. So this concept of like overwriting system bronze is pretty useful for when you want to create an agent with some personality or some specific needs that you need. And another file that's related with systemd is append system.md. That guy will not overwrite completely the original system prompt but we will append to it. Just like for system prompt we also have a CLI argument here that we can pass through pi. So it will append to the system prompt whatever instruction that we have. So here I'm telling the agent to create unit tests and iterate on implementing them until they pass. So if I tell it okay create um script to calculate the Fibonacci sequence invite.

So note it created like a test uh Fibonacci first and then it wrote the script.

it run the Python test and it pass.

Compaction is also available on PI. It has its own autocompact feature, but you can force is through / compact. And uh it's important to remember that compaction is lossy operation. So you cannot reconstruct the a session from the compacted version of it. But you can access all the session history that you had before. Just access the session JSON L file. So skills are also available in PI and they are basically cability packages. So it's um a description of what the tool does and the CLI tools attached to it and you can define that as markdown files and save them inside of PI agent skills with some name that you prefer. And it's basically the same way that you create a skill for cloud code or codeex. Prompt templates are also useful especially when you have like some prompts that you use often and you want to reuse them. Um so you basically create them as markdown files inside of PI agent prompts and they have this support for argument and you can use the argument inside of the markdown just like a template. So if we load that in pi we are going to have the review prompt template available. And if we call it, we can call it like review and uh review in uh test coverage instance and test coverage is going to be inserted in the focus of the template.

Extensions that you have loaded and you create in pi can also change the context dynamically. So that's an important uh aspect while you're considering create your own extension. And one important tip is to have your skills and prompts also under version control so you can share easily between your peers. All right, so let's talk about extensions and uh extensions are pretty core to PI because that's how you can extend its main features and add any kind of functionality that you want. So with extensions you can create your own tools, your own comms, shortcuts, events, compaction strategies. So everything that you want to tweak in pi you can do through extensions. So here we have like some example of how to use the extension API to create an extension.

So we can react to events like here we are reacting to the session start and changing the UI to notify some message to the user. If you want to just like test your extension, a cool tip here is to use minus E to just load the extension in the folder that you have.

You don't have to copy that to the PI agent extensions folder. And here you see that we got the session started event and it display session started. So let's go back to our example. Here in this line we are reacting to the two call. So every time a tool is going to be called, we check if this tool name is a bash and then if it includes some dangerous comment like uh it's just trying to remove some file or some folder and then we block that for user.

So if I run pi now with the extension and I ask it to remove some folder uh so I believe there is a full folder here.

So uh let's ask to remove the full folder. So you can see it was blocked.

So that's one possible way for you to add safeguards in the comments that you run uh in your agent. And you can also define your own tools through the extension API. Like in this case here we are just defining a pretty simple greet tool that you just like greet uh whatever name that we give to the agent.

So if we run the extension inside of pi and now it has the greet to we can say hey Bob is here.

So know that the agent uses the greet and said hello Bob. This is really simple but you can extend that and create any kind of tool and give it programmatically to your agent. And we are defining another simple tool here that's called weather and it will basically call the weather in uh URL and encoding the city that I give as a argument because we can have like arguments like parameters to the to the tool and u it will return the weather for some given city. If you run the extension and ask for the agent uh what's the weather in London, it used the weather tool and gave us um the weather in London which is kind of accurate. It's about to rain again. And one of the cool things about Pi is that because um all the documentation is made for agents too because one of the core features is to let Pi build its own extensions when you ask it to do. The documentation is really well written and there is lots of examples. So if we enter in the pyono ripple, go to packages and then coding agent and examples and extensions. You're going to see lots of examples here for you to tweak and get inspiration to create your own. So I really recommend checking the extension examples and um to have an idea what is possible to do with extension pie. A quick note here. If you created a pretty cool extension and you want to package that, you can package as an npm package or as a git ripple and that will allow you to install the extension in whatever pi session that you have. Since that we are talking about pi packages, it's important to know how to install them.

And uh to install you install from npm or from uh some gle.

So here I'm just like trying to install pyoom and uh it's just like pi install in the name of the repository that we have. So if you run pi now and u we see that there is this pyoom extension loaded and that extension exposes this doom comment.

If we run you see that we have doom here which is pretty useful. Can also list the packages you have. You can also remove some packages if you want to and uh the package should not be available anymore and you can also update and config some specific package if it provides configuration. PI packages will be installed in the git sub folder inside of PI agent or as a global npm.

But you can also install the package inside of your local.py sub folder using the minus L argument. Pi can be customized not only by the extension but the look and feel can be tweaked a bit too and we have teams for that. The teams are stored in PI agent teams and we can access them by slash settings. So if you run pi and go for settings and if you go down here in team you can see that we have the dark team selected and we can select between the other team that they have which is only light but we can create our own team if you want.

create uh matrix inspire team for pi.

While it's creating, it's interesting to see here how it's accessing the documentation that it has about teams and that we going to use that to create its own team. Okay, so it finished creating the team. So let's see if it's in settings here now. So if you go to team matrix terrain. Yeah, that looks pretty green and pretty matrix. So until now we only use pi in interactive mode. But we can use pi in other ways too. One of way is to use as a print and that is pretty interesting to use when you want to combine that with other bash comments.

To use the print mode, you just use the minus p argument and you provide the prompt that you want. just because I'm super creative. Let me ask for what kind of files are in this folder.

Files in this folder.

Pretty convenient because then you can pipe it. For instance, if you have a error file or some log that you want to inspect and then you can pipe it to pi and ask to interpret the result of that log. Another way to interact with PI is to use the JSON event stream mode and to output all the sessions that you have as JSON lines to the standard output. And to access the JSON event stream, you just specify the mode as JSON and pass

And you're going to see the stream of JSON that the agents is providing for us. and together with JSON event streams RPC is another possibility for you if you want to integrate pi in a non NodeJS app have an example how to do that so let's say that I want to communicate a Python script with pi so here I'm running pi as a subrocess in the RPC mode and I'm sending whatever prompt I want to the standard input in this case I'm just saying hello as a message and I'm then iterating through the message that they get back to the from the standard output. So if you run this script, you're sending hello and we are streaming back the output. And the last way to interact with pi is through its SDK. And that's a pretty cool and powerful way because we can really create your own Pi if you want from scratch just by using the functions and classes that are exported by Pi. So here I have like a really really simple example that is just like creating an agent session and writing some prompt to it and getting back the result. So if you run the script we are going to have the files listed because I asked again to list the files and also the JSON output that we had the user message the assistant messages and all. So with RPC, with JSON stream, with a print mode, with the SDK, you can really hack your way into Pi. To help you on that, I really recommend again to check the documentation, especially the docs in the coding agent and the examples because you have all the docs for the SDK, for the extensions, and all the examples that use both. So really recommend to check those and uh play with them. Because it's a open source project, Pi is attracting lots of developers. And we already have like different versions of Pi, other flavors and setup scripts that make your life easier when you start with Pi. You can just discover Lazy Pi. So if you access that in lazy pi.org, I'll leave the links in the description. So when you run lazy pi, it's going to set up lots of pi packages that will add some support for you for sub agents, for MCP and other interesting extensions that you might need. So it's a really great way to start if you don't want to start using pi from scratch. Another interesting project is Oh my PI and it's a fork of pi with lots of uh tools implemented in rust for efficiency and really interesting like it's expanding too. It's really recommend you to check it out. The community is also importing pi to other languages. So pi agent rust is a part of pi to rest and I just started this protocol modex which is this ai coding hard is implemented in the mojo programming language and grab some expiration from pi 2. I just started it. So if you want to check it out and follow along uh it's available here. I will link the in the description. We did it. I think we covered it. I think that's kind of like grips you an idea of like what Pi is about and u what you can do with it, how how you can explore the features it has, how you can extend, how you can hack PI for your own necessity. If you like this format here like it's kind of content and where we go deep in some tech and dismissify AI, please subscribe. is I'm going to publish more and more and leave your comment if you have like any topic that you want me to cover more in a future video. So that's it. I hope you enjoy it and uh see you in next one.
